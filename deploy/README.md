# 月棱雷诺曼部署包

这个文件夹只包含线上发布需要的静态文件：

- `index.html`
- `styles.css`
- `app.js`

推荐固定部署方式：

1. GitHub Pages：创建公开仓库，把这三个文件放到仓库根目录，启用 Pages。
2. Vercel：新建静态站点项目，选择这个文件夹部署。
3. Netlify：新建站点并上传这个文件夹。

不需要构建命令，也不需要安装依赖。
